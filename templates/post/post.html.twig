{% extends 'base.html.twig' %}

{% block body %}
    <div class="mt-4">
        <h3>{{ title }}</h3>
        {{ form_start(form) }}
            <div class="form-group">
                {{ form_label(form.title) }}
                {{ form_widget(form.title) }}
            </div>
            <div class="form-group">
                {{ form_label(form.content) }}
                {{ form_widget(form.content) }}
            </div>
            <button type="submit" class="btn btn-primary mt-4">{{ buttonText }}</button>
        {{ form_end(form) }}
    </div>
{% endblock %}
